<%= colourize('>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>', :yellow) %>
Feed updated: <%= colourize(context.first.display_feed_time, :red) %>
<% context.each do |station| %>
----- <%= colourize('Bike station', :blue) %>
<% station.each_pair.each do |attr_name, attr_value| %>
<%= attr_name.capitalize %>: <%= colourize(attr_value, :cyan) %>
<% end %>
Link to map: <%= colourize(station.map_link, :pink) %>
<% end %>
<%= colourize('>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>', :yellow) %>
